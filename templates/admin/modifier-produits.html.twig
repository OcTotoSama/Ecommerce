
{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://bootswatch.com/5/sandstone/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('css/base.css') }}">
    <link rel="stylesheet" href="{{ asset('css/modifierProduit.css') }}">
{% endblock %}

{% block title %}
    {{ parent() }}Modifier le produit
{% endblock %}

{% block body %}

<div class="product-page">

    <h1 class="product-form-title">
        Modifier le produit
    </h1>

    <div class="product-form-container">

        {{ form_start(form) }}

            <div class="form-group-custom">
                {{ form_label(form.name) }}
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group-custom">
                {{ form_label(form.designation) }}
                {{ form_widget(form.designation, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group-custom">
                {{ form_label(form.image) }}
                {{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group-custom">
                {{ form_label(form.prix) }}
                {{ form_widget(form.prix, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="submit-container">

                <button class="submit-btn" type="submit">
                    Modifier le produit
                </button>

            </div>

        {{ form_end(form) }}

        <div class="back-button-container">

            <a class="back-btn" href="{{ path('app_liste_produits') }}">
                Retour à la liste des produits
            </a>

        </div>

    </div>

</div>

{% endblock %}